[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "litert-cli"
version = "0.1.1.dev17"

description = "LiteRT Toolkit Command Line Interface"
dependencies = [
    "click",
    "numpy",
    "immutabledict",
    "huggingface_hub",
    "requests",
    "ai-edge-litert",
    "Pillow",
]

[project.optional-dependencies]
test = [
    "absl-py",
]
compile = [
    "ai-edge-litert-sdk-qualcomm-nightly",
    "ai-edge-litert-sdk-mediatek-nightly",
    "ai-edge-litert-sdk-google-nightly",
]
convert = [
    "litert-torch-nightly",
    "torch",
    # Latest transformers break litert-torch-nightly
    "transformers<5.0.0",
    "torchvision",
    "sentencepiece",
]
quantize = [
    "ai-edge-quantizer",
]
visualize = [
    "ai-edge-model-explorer"
]
lm = [
    "litert-lm-nightly",
]
all = [
    "litert-cli[compile,quantize,convert,visualize,lm]"
]

[project.scripts]
litert = "litert_cli.litert:cli"

[tool.setuptools]
packages = ["litert_cli", "litert_cli.commands", "litert_cli.core"]

[tool.setuptools.package-data]
"litert_cli" = ["test_data/*"]
"test_scripts" = ["*"]
